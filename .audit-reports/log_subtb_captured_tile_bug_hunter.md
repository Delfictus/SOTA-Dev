# Log-SubTB Captured Tile Bug Hunter

VERDICT: PASS

Final bug-hunter status:
- No CRITICAL findings remain.
- No HIGH findings remain.

Closed findings:
- Full mode formerly used a synthetic demo operator. It now requires a restricted C6 operator artifact generated by `prism-forge/log_subtb_tile_guard`.
- Rust formerly only validated a Python-built registry. It now owns full-mode registry/operator generation via `--build-from-variant-panel`.
- Production metrics formerly repeated one tile sequence/state. The current 500-epoch artifact has nontrivial diversity: `tile_sequence_n_unique=382`, `state_hash_n_unique=382`, `operator_hash_n_unique=183`.
- Colluding C6 operator/registry forgery formerly passed when hashes were self-consistent. Full mode now regenerates canonical Rust artifacts from the current variant panel and rejects non-canonical registry/operator payloads.

Adversarial recheck evidence:
- Colluding forged operator+registry rejected with `LOG_SUBTB_BLOCKED_TILE_CAPTURE: registry is not canonical Rust output`.
- Missing registry in full mode fails closed and does not create a registry.
- Bad operator owner is rejected.
- Stale operator rows are rejected by operator hash recomputation.
- Registry/operator hash mismatch is rejected.
- Tampered registry hashes are rejected by both Python loader and Rust guard.
- Forged `cuda_graph_id` is ignored on load and not counted as captured.
- Failed replay rolls back live BSR values and hashes.
- Forged reward event not produced by captured tile runtime is rejected.
- CPU placement and uncaptured fallback are rejected in production mode.

Final gate evidence:
- Focused pytest: 15 passed, 1 warning.
- Focused strict mypy: success, no issues in 12 files.
- Rust guard tests: 3 passed.
- Rust guard clippy with `-D warnings`: pass.

Residual notes:
- A historical Track B release tarball exists in `/mnt/storage/prism-scratch`, but its timestamp predates this captured-tile addendum. It is not a new package/seal from this directive.
