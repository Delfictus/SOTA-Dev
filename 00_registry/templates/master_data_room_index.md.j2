# PRISM GLP-1R M2 Master Data Room

Campaign ID: `{{ campaign_id }}`

Release date: {{ release_date }}

Campaign Merkle Root Hash:

```text
{{ cbom.campaign_merkle_root }}
```

## 1. Executive Dossier

- Pharmacological Dynamics Intelligence Report: `{{ executive_dossier }}`
- Enterprise Positioning Summary: `{{ enterprise_positioning }}`
- Claim Falsification Graph: `{{ claim_graph }}`

## 2. Interactive 3D Visualizer

Static visualizer entry point: `{{ visualizer_index }}`

The visualizer separates observed tensors from derived, inferred, projected, and hypothesized layers through the epistemic overlay controls.

## 3. Medicinal Chemistry Action Center

Top zero-shot replacement candidates are projected chemistry hypotheses generated by rigid-body grafting and tensor-field scoring.

| Rank | Anchor | SMILES | SA Score | Pi Complement | Pi Clash | Projected Durability Improvement |
|---:|---|---|---:|---:|---:|---:|
{% for row in teaser_solutions -%}
| {{ row.solution_rank }} | {{ row.anchor_id }} | `{{ row.canonical_smiles }}` | {{ "%.2f"|format(row.sa_score) }} | {{ "%.4f"|format(row.pi_complement) }} | {{ "%.4f"|format(row.pi_clash) }} | {{ "%.2f"|format(row.projected_durability_improvement * 100.0) }}% |
{% endfor %}

Dynamic pharmacophore/SAR contingency rows:

### Universal Growth Vectors

{% if universal_growth_vectors|length == 0 -%}
**No universal growth vectors met the conserved thermally_activated cavity criterion. All current exit vectors exhibit high epistemic uncertainty ($U_{pose}$) and require scaffold rigidification before expansion.**
{% else -%}
| Rule | Vector | Edge | Cluster | Atom | Score | Variant support | Rationale |
|---|---|---|---:|---|---:|---:|---|
{% for row in universal_growth_vectors -%}
| {{ row.rule_id }} | {{ row.vector_id }} | {{ row.edge_label }} | {{ row.cooperative_cluster_id }} | {{ row.atom_label }} | {{ "%.4f"|format(row.vector_score) }} | {{ row.variant_family_support_count }} | {{ row.rationale }} |
{% endfor %}
{% endif %}

### Epistemic Mirage Vectors

| Rule | Vector | Edge | Atom | U_pose | Threshold | Rationale |
|---|---|---|---|---:|---:|---|
{% for row in epistemic_mirage_vectors -%}
| {{ row.rule_id }} | {{ row.vector_id }} | {{ row.edge_label }} | {{ row.atom_label }} | {{ "%.4f"|format(row.atom_edge_u_pose) }} | {{ "%.4f"|format(row.epistemic_u_pose_threshold) }} | {{ row.rationale }} |
{% endfor %}

### Prohibited Rigidification Zones

| Rule | Vector | Edge | Classification | Score | Clash integral | Shear integral | Rationale |
|---|---|---|---|---:|---:|---:|---|
{% for row in prohibited_vectors -%}
| {{ row.rule_id }} | {{ row.vector_id }} | {{ row.edge_label }} | {{ row.classification }} | {{ "%.4f"|format(row.vector_score) }} | {{ "%.4f"|format(row.pi_clash_integral) }} | {{ "%.4f"|format(row.shear_stress_normalized_integral) }} | {{ row.rationale }} |
{% endfor %}

## 4. Wet-Lab CRO Handoff

Each row is framed as a falsification gate rather than a confirmation request.

| Assay | Condition | Residue | Peptide Window | Construct | Measurement Readout | Execution Instruction | Falsification Condition |
|---|---|---|---|---|---|---|---|
{% for row in cro_rows -%}
| {{ row.assay_category }} | {{ row.condition_id }} | {{ row.residue_name }}{% if row.residue_idx is not none %}:{{ row.residue_idx }}{% endif %} | {% if row.peptide_start_residue_idx is not none %}[{{ row.peptide_start_residue_idx }}, {{ row.peptide_end_residue_idx }}]{% else %}Variant/construct level{% endif %} | {{ row.construct }} | {{ row.measurement_readout }} | {{ row.execution_instruction }} | {{ row.falsification_condition }} |
{% endfor %}

## 5. Track A Cloud AI Readiness

| Component | Status |
|---|---|
{% for row in track_a_status -%}
| {{ row.component }} | {{ row.status }} |
{% endfor %}

Cloud infrastructure architecture: `{{ cloudflare_architecture }}`

## 6. Pending GPU Campaigns

| Campaign | Artifact | Status |
|---|---|---|
{% for row in pending_gpu_campaigns -%}
| {{ row.name }} | `{{ row.path }}` | {{ row.status }} |
{% endfor %}

## 7. Cryptographic Bill of Materials

| Field | Value |
|---|---|
| CBOM schema | `{{ cbom.schema_version }}` |
| File count | {{ cbom.file_count }} |
| Directory count | {{ cbom.directory_count }} |
| Python | `{{ cbom.environment.python_version }}` |
| Polars | `{{ cbom.environment.polars_version }}` |
| OS kernel | `{{ cbom.environment.os_kernel }}` |
