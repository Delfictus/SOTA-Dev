# PRISM-DSTW Engineering Status Report

Generated: 2026-05-21T19:21:21-07:00

Source audit: `PRISM-DSTW_State_of_the_Union.md`

Scope:

- Repository: `/home/diddy/Desktop/Prism4D-bio`
- Active GLP-1R generated artifacts: `/home/diddy/Desktop/PRISM-DSTW/prism-dstw-calibration/campaigns/glp1r_aleniglipron`
- Audit mode: read-only verification findings synthesized into engineering status

## 1. Executive Status

Overall classification: [Partially Implemented]

PRISM-DSTW has crossed an important engineering threshold: the active GLP-1R receptor-side evidence path is now materially organized, provenance-stamped, and isolated from direct executable imports of the 314 tracked legacy-contaminated files. The system is no longer only a set of reports. It has active generated temporal, voxel, hydration, SAR, and path-sampling target artifacts that can support a bounded Track 0 receptor-side commercial audit.

The system has not yet crossed the threshold into full Scaffold-State Interferometry. The missing core is not cosmetic. The Rust/CUDA ligand projection engine, bivariate clash/complement field convolution, analog-conditioned DTSG emission, `U_pose` propagation, Graph-Wasserstein DKL, Chem-BALD, and biological chronic durability evidence layers are still absent or stubbed.

Engineering conclusion:

- Current state supports a disciplined receptor-side GLP-1R durability risk and SAR contingency audit.
- Current state does not support claims of analog-conditioned scaffold-state interferometry.
- Current state does not support claims of full chronic receptor durability across residence, trafficking, coupling, internalization, recycling, degradation, membrane adaptation, and repeated exposure.
- The active path is clean enough to build from, but the upstream lineage and legacy backlog are not fully certified.

## 2. Readiness Snapshot

| Area | Status | Engineering Meaning |
| --- | --- | --- |
| Active GLP-1R artifact layer | [Partially Implemented] | Large receptor-side evidence surfaces exist and are usable with claim boundaries. |
| SAR topology register | [Fully Implemented] | The receptor-side SAR contingency topology is materialized and quantified. |
| Temporal event support | [Partially Implemented] | Transition candidates exist, but interface-breaking timestamps require executed path sampling. |
| Dynamic voxel layer | [Partially Implemented] | Spatial/time voxel evidence exists, but producer-canonical variance is not yet available. |
| Hydration statistics | [Fully Implemented] | Event, site, residue, and interface hydration statistics are present by ontology class. |
| Path-sampling launch queue | [Partially Implemented] | Ranked windows and launch queues exist; trajectory execution is not done. |
| Provenance-stamped parquet writing | [Partially Implemented] | Active regenerated parquets carry Polars provenance and many ledgers. |
| Propagation ledger | [Stubbed] | Cryptographic lineage exists, but mechanistic parameters and uncertainty are not propagated. |
| Active path legacy isolation | [Fully Implemented] | No direct executable import/call contamination from the 314 tracked legacy files. |
| Full upstream lineage certification | [Partially Implemented] | Historical producers and several adjacent artifacts still require recertification. |
| Rust ligand projection core | [Missing] | Required field convolution and edge attenuation modules do not exist. |
| CUDA Scaffold-State kernels | [Missing] | Existing CUDA is not the required ligand field/edge attenuation/Sinkhorn workload. |
| Rust ontological newtypes | [Missing] | Rust boundaries still use primitive numerical types for domain quantities. |
| Python ontology | [Partially Implemented] | NewTypes and dataclasses exist but are not active enforcement through producers. |
| Schema validation | [Partially Implemented] | Schema validator exists, but write boundaries do not universally invoke it. |
| DKL/Bayesian layer | [Missing] | Graph-Wasserstein GP, Pyro/GPyTorch, Tobit, EiV, and Chem-BALD are not implemented. |
| Track 0 revenue report | [Partially Implemented] | Receptor-side report can be assembled; analog-conditioned report cannot ship yet. |

## 3. Active Evidence Inventory

### 3.1 SAR Topology

Status: [Fully Implemented]

Materialized evidence:

- 11 target hinges.
- 145 inactive-only lock interfaces.
- 9 sterically feasible wedge interfaces.
- 4 primary pocket-accessible vectors.
- 5 allosteric downstream lock surfaces.
- 136 long-range rejected correlations.

Engineering interpretation:

This layer is mature enough to support receptor-side SAR contingency guidance. The SAR funnel is explicit: inactive-only lock interfaces are filtered by steric feasibility and pocket accessibility. This is suitable for Track 0 report use if every claim is framed as computational receptor-side evidence, not experimental confirmation.

Scientific boundary:

This topology nominates plausible SAR vectors and allosteric endpoints. It does not prove that a specific ligand analog attenuates or amplifies a causal wire because analog-conditioned ligand field projection is not implemented.

### 3.2 Temporal Event Support

Status: [Partially Implemented]

Materialized evidence:

- `interface_time_bins.parquet`: 1,511,577 rows.
- `support_intervals.parquet`: 63,072 rows.
- `state_transitions.parquet`: 126,000 rows.

Engineering interpretation:

The system can now identify high-value transition windows and event-support intervals. This is enough to rank localized path-sampling targets and define the computational agenda for interface-breaking timestamp extraction.

Scientific boundary:

These are not interface-breaking timestamps. They are event-support candidates. The timestamp layer becomes mechanistic temporal evidence only after localized path-sampling trajectories are executed and joined back to interface state changes.

### 3.3 Dynamic Aligned Voxel Layer

Status: [Partially Implemented]

Materialized evidence:

- `dynamic_voxel_event_time_bins.parquet`: 27,661,810 rows.
- `interface_aligned_voxel_fields.parquet`: 544,976 rows.
- `site_aligned_voxel_fields.parquet`: 86,680 rows.

Engineering interpretation:

This is one of the most important current assets. It converts spike/time evidence into spatially indexed receptor regions that can be used for pocket, interface, and voxel-level burden analysis. It gives the Track 0 report a credible spatial substrate beyond residue-level tables.

Scientific boundary:

The current layer is not yet canonical per-frame voxel occupancy with canonical producer-side variance. It is usable as aligned dynamic evidence, but not as final constructive-interference proof.

### 3.4 Stable/Void Proxy Layer

Status: [Partially Implemented]

Materialized evidence:

- `voxel_stable_void_proxy.parquet`: 631,656 rows.
- High-variance void proxy rows: 338,111.
- Stable occupied proxy rows: 1,018.

Engineering interpretation:

This layer gives an immediate proxy route for destructive versus constructive interference classification. It is correctly useful only when labeled as proxy-derived. The recent stream-local denominator tightening is the correct direction because stream mixing at threshold time would contaminate the local ontology.

Scientific boundary:

Constructive interference remains proxy-derived until `signal_grid.bin` or the producer-side voxel stream emits canonical stable-occupied versus high-variance-void classification. Any report using this layer must label the field as empirical proxy evidence, not canonical ligand complementarity physics.

### 3.5 Hydration Statistics

Status: [Fully Implemented]

Materialized evidence:

- `event_time_hydration_bins`: 167,953 rows.
- `site_time_hydration_bins`: 3,463,339 rows.
- `residue_time_hydration_bins`: 21,170,046 rows.
- `interface_time_hydration_bins`: 1,419,745 rows.

Engineering interpretation:

Hydration evidence is available at multiple ontology classes. This is important because averages must not be computed across mixed ontology scopes. Event, site, residue, and interface statistics need separate denominators and separate report language.

Scientific boundary:

Hydration burden supports receptor-side mechanistic interpretation. It is not a substitute for ligand residence, trafficking, internalization, recycling, or chronic cellular adaptation data.

### 3.6 Localized Path-Sampling Launch Layer

Status: [Partially Implemented]

Materialized evidence:

- `localized_path_sampling_ranked_windows.parquet`: 126,000 rows.
- `localized_path_sampling_launch_queue.parquet`: 864 rows.

Engineering interpretation:

The launch queue is a concrete transition from broad evidence mining into executable mechanistic sampling. It identifies where computational effort should be spent to extract interface-breaking timestamps.

Scientific boundary:

No localized path-sampling trajectories have been executed yet. The system is ready to launch targeted runs only after runtime/storage planning and queue validation. It cannot yet claim extracted breaking times.

## 4. Lineage and Provenance Status

### 4.1 Active Parquet Provenance

Status: [Partially Implemented]

Findings:

- Active regenerated parquets use Polars provenance metadata.
- Examples include `interface_time_bins.parquet`, `dynamic_voxel_event_time_bins.parquet`, `voxel_stable_void_proxy.parquet`, and `localized_path_sampling_ranked_windows.parquet`.
- Provenance metadata records writer identity and related source information.

Engineering interpretation:

This is a meaningful improvement. The active path is no longer relying on silent file generation. There is enough metadata to begin certification of active generated outputs.

Remaining gap:

Provenance metadata is not the same as complete computational lineage. It tells us what wrote the file and what inputs were declared. It does not yet prove that every scientific threshold, partition rule, parameter, and uncertainty transformation is traceable.

### 4.2 Propagation Ledger

Status: [Stubbed]

Findings:

- 19 `.propagation.jsonl` files were found across active output directories.
- Entries contain non-empty `input_checksums`, `parameters`, and `gate_status`.
- The captured parameters are writer/provenance-level parameters:
  - `compression`
  - `partition_keys`
  - `pipeline_stage`
  - `row_group_size`
  - `schema_version`
- All inspected `output_uncertainty` fields are `null`.
- No `U_pose` chain is present.
- Scientific thresholds and beta constants are absent from ledger entries.

Engineering interpretation:

The ledger is append-only and cryptographic in spirit, but not yet mechanistic. It proves that outputs were written with source checksums. It does not yet prove how the output values were computed in a scientifically auditable way.

Required promotion criteria:

- Ledger entries must capture all module-specific thresholds.
- Ledger entries must capture `beta_f`, `beta_s`, and calibration labels where applicable.
- Ledger entries must capture proxy labels such as `proxy_voxel_variance=true`.
- Every numerical output used in a deliverable must include `output_uncertainty` or an explicit `uncertainty_unavailable_reason`.
- `U_pose` must propagate from Layer 1 through DTPF, CP/SFI, and deliverables once Layer 1 exists.

### 4.3 Legacy Isolation

Status: [Fully Implemented] for active executable import isolation.

Findings:

- `audit/workspace_purge_manifest.md` tracks 314 contaminated files and 5,688 line-bound violation instances.
- Active scripts checked:
  - `scripts/prism_interface_timestamp_miner.py`
  - `scripts/prism_dynamic_aligned_voxel_export.py`
  - `scripts/prism_hydration_event_statistics.py`
  - `scripts/prism_path_sampling_launcher.py`
  - `scripts/prism_voxel_variance_proxy_classifier.py`
- No direct executable import or call into the tracked legacy files was found.

Engineering interpretation:

The active path is clean enough to continue hardening. This is not the same as full repo cleanliness. It means new active work can be kept outside the contaminated backlog if the gates remain strict.

Remaining warning:

One non-executable docstring reference points to `prism_spike_event_integrator.py`, a legacy-contaminated producer. This is not runtime contamination, but it is a lineage warning. Upstream data production remains a certification problem.

## 5. Rust and CUDA Execution Core

### 5.1 Required Scaffold-State Rust Modules

Status: [Missing]

Required but absent:

- `crates/prism-nhs/src/dstw_dispatch/ligand_field.rs`
- `crates/prism-nhs/src/dstw_dispatch/field_convolution.rs`
- `crates/prism-nhs/src/dstw_dispatch/edge_attenuator.rs`
- `crates/prism-nhs/src/dstw_dispatch/parquet_emitter.rs`

Audit finding:

`field_convolution.rs` and `edge_attenuator.rs` do not exist. The current Rust DSTW projection path is a CPU variant/rotamer projection path, not the ligand field projection and bivariate DTSG attenuation engine required by Scaffold-State Interferometry.

Engineering implication:

The central physics execution core is not implemented. Until this exists, downstream modules can only operate on receptor-side evidence or manually assessed analog overlays. They cannot produce `Chem_Perturbed_DTSG.parquet`, `U_pose`, or computed DTPF fields.

### 5.2 Current Rust Projection Path

Status: [Partially Implemented]

Findings:

- `projection.rs` contains CPU projection logic.
- It uses raw `Vec<f64>` arrays.
- It uses serial loops and iterator variance.
- It does not implement fused K-conformer edge accumulation.
- It does not implement Welford O(E) accumulation for `U_pose`.
- It projects variants/rotamers rather than ligand conformer fields over receptor voxels.

Engineering implication:

The existing code is valuable as prior infrastructure, but it is not the production hot path required by the current specification.

### 5.3 CUDA/GPU Status

Status: [Partially Implemented] for general CUDA infrastructure; [Missing] for required Scaffold-State kernels.

Findings:

- CUDA exists in the repo.
- A DSTW propagation kernel exists under `crates/prism-gpu/src/kernels/dstw_propagation.cu`.
- `crates/prism-nhs/src/dstw_dispatch/cuda.rs` can load/launch a propagation kernel.
- The live dispatcher remains CPU-oriented and treats CUDA as deferred.

Missing required kernels:

- `field_convolve.cu`
- `edge_attenuate.cu`
- `wasserstein_sinkhorn.cu`

Engineering implication:

The repo has GPU capability, but not the GPU capability the spec requires for batch ligand field convolution, batch DTSG attenuation, or Graph-Wasserstein/Sinkhorn acceleration.

### 5.4 Rust Ontological Types

Status: [Missing]

Findings:

- Rust still uses primitive `u32`, `i32`, `f32`, and `f64` at DSTW/Arrow/Parquet boundaries.
- Requested newtypes such as `ResidueIdx`, `CausalCoupling`, `FrustrationPenalty`, and `PoseUncertainty` are not enforced.

Engineering implication:

The Rust layer remains vulnerable to ontological confusion because semantic quantities can be numerically interchanged. The Python ontology layer cannot compensate for missing Rust boundary types because Rust is the intended producer of the canonical tensors.

## 6. Python Ontology, Schema, and Math Discipline

### 6.1 Python Ontology

Status: [Partially Implemented]

Present:

- Identifier NewTypes:
  - `CampaignId`
  - `RunLabel`
  - `StructureId`
  - `StreamId`
  - `ResidueIdx`
  - `EdgeIdx`
  - `ConformerIdx`
  - `AnalogIdx`
  - `ScaffoldIdx`
  - `VoxelIdx`
- Physics NewTypes:
  - `CausalCoupling`
  - `HysteresisEnthalpy`
  - `HydrationVariance`
  - `SpatialVariance`
  - `FrustrationPenalty`
  - `ComplementPenalty`
  - `PoseUncertainty`
  - `ScalingConstant`
  - `AngstromDistance`
- Dataclasses:
  - `PartitionKey`
  - `DTSGEdge`
  - `PerturbedEdge`

Gap:

Active producers and writer functions mostly use primitive `str`, `Path`, `float`, and `dict[str, Any]`. The NewTypes are defined but not yet making illegal states unrepresentable in function signatures.

Engineering implication:

The ontology exists as a vocabulary. It is not yet fully operationalized as a type-enforced contract.

### 6.2 Schema Validation

Status: [Partially Implemented]

Present:

- `prism_dstw/schema.py` can validate column presence, mapped dtype, and nullability for mapped columns.
- Schema YAML files can carry ontology and unit metadata.

Gaps:

- Active producers do not universally call schema validation before writes.
- Ontology and unit tags are not yet enforced as runtime semantic contracts.
- LazyFrame nullability checks still require execution.

Engineering implication:

Schema validation exists but must be moved into every read/write boundary to become a gate rather than an optional utility.

### 6.3 Mypy Strictness

Status: [Missing] in the current environment.

Finding:

`mypy` is not installed, so `mypy --strict` could not be run against `prism_dstw/io.py`, `prism_dstw/ontology.py`, or `prism_dstw/propagation_ledger.py`.

Engineering implication:

Python type discipline cannot be certified until the toolchain includes mypy and the CI gate runs it.

### 6.4 Numerical Discipline

Status: [Partially Implemented]

Present:

- Ban check catches some known naive numerical forms, including direct matrix inverse and determinant usage.

Gaps:

- No property-based numerical stability suite was verified.
- No full cross-repo enforcement was verified for KL divergence, log-sum-exp, softmax, entropy, Gaussian log-probability, Wasserstein distance, or float comparison discipline.

Engineering implication:

The repository has pattern-level prevention for some known issues. It does not yet have mathematical validation coverage sufficient for the full Workspace Engineering Standard.

## 7. ML, Bayesian, and Active Learning Layer

Status: [Missing]

Not found:

- `src/prism_dstw/hierarchical_bayes/dkl_wasserstein.py`
- Graph-Wasserstein DKL model
- GPyTorch GP kernel over physical graph distances
- Pyro SVI
- Tobit likelihood
- Errors-in-Variables sampling from `N(E[DTSG], U_pose)` in a forward pass
- Chem-BALD qEIG active learner
- Persistent joint manifold store integration for DKL latent vectors

What exists:

- EGNN pocket ranking and Wasserstein post-processing code exists elsewhere.
- Rust variant dispatcher emits sigma-like uncertainty fields.

Engineering interpretation:

The current ML layer is not the Module 6 system described by the specification. It cannot be used to claim Bayesian scaffold optimization, Graph-Wasserstein active learning, or pose-uncertainty-propagated analog predictions.

Required entry condition:

Do not build Module 6 until Layer 1 emits analog-conditioned `Chem_Perturbed_DTSG.parquet` and `U_pose` tensors. Building DKL before canonical inputs exist would create a model over placeholders rather than the specified physical manifold.

## 8. Track 0 Commercial Readiness

### 8.1 What Can Ship Now

Status: [Partially Implemented]

A Track 0 receptor-side manual-emulation audit can be assembled now if it is explicitly bounded.

Allowed content:

- Quiet-thermal lock hypothesis.
- SAR contingency vector register.
- Four primary pocket-accessible vectors.
- Five downstream lock surfaces.
- SAR funnel: 145 inactive-only to 9 steric to 4 pocket-accessible.
- Hydration statistics by ontology class.
- Dynamic voxel burden summaries.
- Timestamp transition candidates.
- Path-sampling launch targets.
- Claim-boundary table.
- Artifact manifest and hash list for included evidence.

Required language:

- "bounded computational receptor-side evidence"
- "event-support transition candidates"
- "proxy-derived stable/void voxel classes"
- "localized path-sampling launch targets"
- "SAR contingency vectors"

### 8.2 What Cannot Ship Yet

Status: [Missing]

Do not claim:

- Full chronic receptor durability.
- Ligand residence time.
- Arrestin recruitment or G protein coupling mechanism.
- Desensitization or internalization behavior.
- Recycling or degradation kinetics.
- Membrane-context adaptation.
- Repeated-exposure biological durability.
- Analog-conditioned causal rewiring.
- Executed interface-breaking timestamps.
- Constructive interference from canonical producer-side voxel variance.

### 8.3 Track 0 Blockers

Status: [Missing] or [Stubbed]

Critical blockers:

1. [Missing] 3-5 aleniglipron analog structures or SMILES.
2. [Missing] Analog activity/selectivity/residence/chronic response annotations.
3. [Missing] Superposition workflow into PRISM pocket site.
4. [Missing] Analyst interference workbook for LEU144-TYR145, LEU144-ILE147, LEU144-ILE146, and PHE230-CYX226.
5. [Missing] Client-ready report package.
6. [Stubbed] Analog interference scoring templates are not populated.
7. [Partially Implemented] Artifact evidence is fragmented across outputs and reports.

Engineering conclusion:

Immediate revenue remains possible, but only by shipping the receptor-side audit with analog intake pending or with a clearly marked analyst-assessed analog appendix if analog data is provided.

## 9. Chronic Durability Gap

Status: [Missing] for full biological chronic durability.

Current PRISM-DSTW receptor-side evidence addresses:

- conformational topology
- thermal/hysteresis-like lock hypotheses
- hydration and voxel burden
- candidate transition windows
- path-sampling target selection

Full chronic receptor durability also requires:

- ligand residence and dissociation kinetics
- receptor conformational cycling under repeated occupancy
- G protein coupling
- arrestin recruitment
- desensitization
- internalization
- recycling
- degradation
- membrane context
- cellular adaptation
- repeated-exposure kinetics over longer biological timescales

Engineering implication:

These biological layers cannot be inferred responsibly from current receptor-side PRISM artifacts alone. They require new data contracts and ingestion layers. PRISM-DSTW can become the organizing manifold for these layers, but it does not currently contain them.

Required future bridge:

- Add assay evidence schemas for BRET/cAMP/arrestin/internalization/recycling/degradation.
- Add residence-time and chronic exposure metadata.
- Add time-aligned biological response joins against receptor conformational states.
- Maintain ontology separation between computational receptor evidence and cellular assay evidence.
- Propagate uncertainty and assay provenance into deliverables.

## 10. Risk Register

### R1. Overclaiming Risk

Status: [High]

Risk:

The current artifact set is rich enough to sound like full durability, but it is not full durability. The engineering report must prevent scientific overreach.

Control:

Every deliverable must include a claim-boundary table distinguishing measured computational evidence, proxy-derived evidence, launch targets, and missing biological evidence.

### R2. Mechanistic Ledger Gap

Status: [High]

Risk:

Propagation ledgers exist but do not capture scientific parameters or uncertainty. This can create a false sense of auditability.

Control:

Promote ledgers from writer provenance to computation provenance before using them as due-diligence audit proof.

### R3. Rust Core Missing

Status: [High]

Risk:

Without Rust/CUDA Layer 1, Scaffold-State Interferometry cannot be computed. Python reports can only describe receptor-side topology or manual analog overlays.

Control:

Build Layer 1 before Modules 1-6 automation. Do not build downstream ML over noncanonical placeholder tensors.

### R4. Ontological Confusion

Status: [Medium-High]

Risk:

Python NewTypes exist but are not active at all producer boundaries. Rust lacks equivalent newtypes. Numeric columns can still be semantically mixed.

Control:

Enforce ontology in schema validation and Rust newtypes before canonical tensor emission.

### R5. Legacy Lineage

Status: [Medium-High]

Risk:

Active scripts do not import legacy files, but upstream historical data may still derive from legacy producers.

Control:

Continue Arrow/Polars-native regeneration and lineage certification. Treat legacy-derived data as uncertified until regenerated or explicitly bounded.

### R6. Commercial Timing

Status: [Medium]

Risk:

Waiting for full Scaffold-State automation delays immediate revenue. Shipping without boundaries risks trust.

Control:

Ship Track 0 receptor-side audit with disciplined language and a clear addendum stating exactly what analog-conditioned automation will add.

## 11. Engineering Critical Path

### Phase A: Audit Closure and Track 0 Packaging

Status target: move Track 0 from [Partially Implemented] to [Fully Implemented] for receptor-side audit only.

Required outputs:

1. Frozen artifact manifest for the client package.
2. Hash manifest for every included parquet/report/figure.
3. Claim-boundary table.
4. SAR funnel table and figure.
5. Top 4 pocket vector table.
6. Top 5 downstream lock table.
7. Hydration and voxel burden summary by ontology class.
8. Timestamp/path-sampling launch-target summary.
9. Analog intake appendix.
10. Final `Receptor_Durability_Audit_V0_1` package.

Acceptance criteria:

- No statement implies biological chronic durability.
- Every number has provenance.
- Every proxy layer is labeled.
- Every missing layer is named.

### Phase B: Layer 1 Rust/CUDA Scaffold-State Engine

Status target: move from [Missing] to [Partially Implemented], then [Fully Implemented].

Required modules:

1. `ligand_field.rs`
2. `field_convolution.rs`
3. `edge_attenuator.rs`
4. `parquet_emitter.rs`
5. Optional CUDA kernels:
   - `field_convolve.cu`
   - `edge_attenuate.cu`
   - `wasserstein_sinkhorn.cu`

Required outputs:

- `Chem_Perturbed_DTSG.parquet`
- `U_pose.parquet`
- emission provenance
- schema validation
- no NaN/null emissions
- Rust ontology newtypes at boundaries
- Welford running variance for conformer ensembles

Acceptance criteria:

- K conformers produce `E[DTSG]` and `U_pose` without allocating K full graph copies.
- Clash and complement terms are separated when canonical voxel variance exists.
- Destructive-only fallback is explicitly labeled when complement is unavailable.
- Parquet emits via Arrow/parquet-rs with schema metadata.

### Phase C: Mechanistic Propagation Ledger

Status target: move from [Stubbed] to [Partially Implemented], then [Fully Implemented].

Required additions:

- scientific thresholds
- physical constants
- beta values
- proxy/canonical labels
- uncertainty values
- downstream uncertainty transformations
- stale/supersedes support
- deliverable-level traceability

Acceptance criteria:

- Every delivered number can be traced backward to source files, parameters, and uncertainty.
- No output uses `output_uncertainty=null` without an explicit reason.

### Phase D: Modules 1-5

Status target: only begin after Layer 1 emits canonical analog-conditioned tensors.

Required order:

1. Module 1 DTPF construction from `Chem_Perturbed_DTSG`.
2. Module 2 durability audit metrics with correct ontology-specific denominators.
3. Module 5 dynamic `DeltaDeltaGapp` persistence with uncertainty.
4. Module 3 SAR drift mapping from Modules 1 and 2.
5. Module 4 side-chain adaptation once motif priors and chronic occupancy evidence exist.

Acceptance criteria:

- Every aggregate denominator is ontology-local.
- No event-level average is mixed with site/residue/interface-level averages.
- All outputs include uncertainty or explicit gaps.

### Phase E: Module 6 DKL/Chem-BALD

Status target: do not start until Modules 1-5 have at least one real analog path.

Required components:

- PyG message passing governed by perturbed `TÊ`.
- Graph-Wasserstein GP kernel.
- Pyro/GPyTorch inference.
- Tobit likelihood.
- EiV sampling from `N(E[DTSG], U_pose)`.
- qEIG batch active learning.
- synthetic accessibility gate.
- persistent D1/R2/Vectorize manifold store.

Acceptance criteria:

- ECE <= 0.15 before deployment.
- Every prediction includes mean and variance.
- Active learner persists every cycle.

### Phase F: Chronic Durability Biological Bridge

Status target: build after receptor-side and analog-conditioned computational layers are stable.

Required evidence contracts:

- residence time
- dissociation kinetics
- G protein coupling
- arrestin recruitment
- desensitization
- internalization
- recycling
- degradation
- membrane environment
- repeated exposure
- cellular adaptation

Acceptance criteria:

- Biological assay data remains ontologically separate from computational receptor state data.
- Joins are time-aligned and provenance-stamped.
- Deliverables distinguish computational mechanism hypotheses from experimental assay observations.

## 12. Decision Boundaries

### Green to Proceed

Proceed with:

- Track 0 receptor-side report assembly.
- Artifact freezing and hash manifests.
- Claim-boundary table.
- Rust Layer 1 design and implementation.
- Propagation ledger expansion.
- Canonical schema enforcement.

### Do Not Proceed Yet

Do not proceed with:

- DKL/Chem-BALD production claims.
- Module 6 implementation over placeholder tensors.
- Full chronic durability deliverables.
- Analog-conditioned claims without analog data and `Chem_Perturbed_DTSG`.
- Constructive interference claims without canonical or clearly labeled proxy stable/void classification.

## 13. Final Engineering Assessment

[Partially Implemented] The current repo has enough real active-path evidence and enforcement scaffolding to support a serious receptor-side Track 0 audit. This is not a toy state. The data volume and ontology-separated outputs are meaningful.

[Missing] The central Scaffold-State Interferometry engine is not implemented. The absence of Rust/CUDA field convolution, edge attenuation, `Chem_Perturbed_DTSG`, and `U_pose` is the principal blocker.

[Stubbed] The propagation ledger is structurally useful but scientifically incomplete. It must move from file-level provenance to computation-level lineage.

[Partially Implemented] The engineering controls are strong enough to stop additional naive work if enforced. They are not yet strong enough to certify the entire workspace because the legacy backlog, missing mypy toolchain, and incomplete schema/ontology enforcement remain.

[Partially Implemented] Immediate commercial delivery is viable only as a bounded receptor-side durability risk audit and SAR contingency register. That can be valuable, but only if the report is explicit about what is measured, what is inferred, what is proxy-derived, and what remains missing.

